{
  "name": "projects",
  "version": "1.0.0",
  "description": "Vanilla TypeScript application with Vite (HTML + CSS + TS)",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "preinstall": "npx only-allow pnpm",
    "lint": "eslint",
    "start": "vite preview",
    "ts-check": "tsc -p tsconfig.json",
    "deploy": "pnpm install && vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.95.3",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "coze-coding-dev-sdk": "^0.7.16",
    "eslint": "^9",
    "gh-pages": "^6.3.0",
    "only-allow": "^1.2.2",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8",
    "vite": "^7.2.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.27.2"
    }
  }
}
